\begin{figure}[!t]
    \centering
	\includegraphics[width=\linewidth]{figures/why_masking.pdf}
    \vspace{-5mm}
    \caption{\textbf{细化调度的动机}。给定每个图像，其中不同百分比的视觉token被掩码，训练的\model被要求完成剩余的token。注意从90\%到95\%的突然全局变化。\lu{I found this figure confusing. Why it suddenly change everything at the last 5\%. This is kind of against the example you have in Figure 1 and the general story. We should consider removing it if it is only used to show why masking is important. (after ddl we should talk about this behavior more.)}}
    \label{fig:why_masking}
\end{figure}